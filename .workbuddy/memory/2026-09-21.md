# 2026-09-21

- 为每日双时点接口在连接器现有 __main__ 下补充真实DB测试：输出约束、逐日pandas独立对照SQL、日期轴覆盖、单日一致性和反向日期拒绝。空数据停止、不构造样本；只执行AST检查，运行断言由用户自行执行。

- 实现 DBConnector.get_daily_market_counts：SQL 参数化只读聚合，按长表日历紧邻前日close比较，量额正值过滤，每日open/close两行；开盘限制数量填0并标不可用，收盘按池记录计数，缺失填0。增加活跃/有效方向对象数与状态列，main加入调用，文档同步。AST通过，新增SQL未执行，明确仅事后统计。

- 每日计数需求新增开盘/收盘双时点、收盘价比较方向、先排除停牌、源缺失强制0。原文档就近整理确认项并补问停牌判定、前日参考、开盘涨跌停快照缺口和两行返回形式；指出日终量额过滤开盘样本有前视问题，暂未实现。

- 完善数据库说明末尾 demand 的每日涨跌及涨跌停计数需求：建议SQL聚合、统一样本范围、方向字段核验、异动覆盖未知不填零；明确接口返回和真实数据调试要求，保留三个待确认问题。未改代码或查询数据库；记录 demand 标记约定。

- 用户授权将本机默认凭据写入连接器便于直接调试；已设置密码默认值，显式参数及环境变量仍优先覆盖。记忆与说明不记录凭据值。未执行实际数据库查询。

- 整理 DB 说明为连接信息、表清单、字段待补区、候选关联、口径核验与后续材料六部分；原稿为 4 类、5 张表，非实际查询计数。
- 未连接 DB、未补造字段；覆盖日期和数量均区分原稿描述与待核验事实。明确 T2 汇总需检查覆盖重叠及单位，T1/T4 需检查重叠区间，T3 需确认历史有效期。
- 从说明移除明文凭据，不在记忆中存储；在 R1 登记 T7 代号。整理稿词表检查通过。
- 用户确认本机后完成只读 DB 核验：实际可见 6 张表、96 个字段；补齐类型、可空性、默认值、键结构、精确行数及日期覆盖。发现额外 T5，T3 无历史有效期字段，T5/T7 日期数量不同；未修改 DB 数据或结构。
- 原字典已更新，另提供可查看副本与不含凭据的字段映射代码。单位、adj、重叠数据一致性和缺失率未验证，不能视为业务质量已通过。
- 用户明确纠正脱敏边界：仅对话文字脱敏；项目代码、配置及 Markdown 等文件均不脱敏，应使用真实名称与清晰术语。已更新 R1 适用范围及项目长期约定；凭据仍不进入普通文档。
- 按用户要求将数据模块数据库说明精简为连接信息、6 张表的内容、96 个真实字段（同类合并展示）及单位，删除代号和约束审计细节；保留待用户确认的单位/口径及后续脚本接口区，不新增副本。
- 根据用户在数据库说明第四节的回复更新原文：四个指数代码、指数成交量为股/成交额为元、价格与前复权约定、后续仅使用长表、指标由脚本计算不回写及 OBV 弃用。列明4表8个百分比字段位置供用户检查；比例、汇总范围及股票范围仍待确认。
- 用户补充百分比口径后，已更新数据库说明：指数换手率无数据弃用；长表 pct_chg 无数据弃用、turnover_rate 以小数比例存储；异动表 pct_chg 与 turn 以百分数值存储；短表相关字段暂不套用长表口径。
- 按用户要求在数据库说明末尾追加接口实现前的问题及回复位置，涵盖行业/概念来源、成分返回格式、参数与列表顺序、周线聚合、调试示例。保留原接口需求，等待用户在文档中确认后实现。
- 用户确认概念方案后，在现有 DBConnector 实现六个只读接口及 __main__ 小范围调用示例，原数据库说明同步参数返回与边界约定。周换手率采用累计日比例并明确非去重比例、缺失不补零；该口径为实现解释。AST 静态检查通过，未执行接口或调试入口，待用户验证。
- 根据实际文件清单，在根 README 末尾追加目录分类与职责：核心数据流、业务模块、配置入口验证、输出与协作目录、查阅入口；区分占位与已有实现，注明 outputs 为辅助产物、正式实验结果遵循 results/traces 约定。未迁移或删除文件。
- 按用户要求整理数据库说明：删除原始接口问答及重复需求，将分类选择与跨分类成员关系归入接口约定；末尾保留60日翻倍需求的技术建议和待定口径，不将建议标记为已确认或已实现。
- 用户明确禁止未经允许构造测试数据；已移除特征模块 main 中的人工数据与断言，改为真实 DBConnector 取数、全表日期并集与59日前置日期预热。查询无数据即停止；长期约定与模块说明同步。静态语法通过，未运行数据库筛选。
- 根据用户在数据库说明末尾的新问题，补充模块归属和记录方式：解释 data 仅取数、preprocessing 计算派生特征；当前翻倍函数每 symbol 一行、只保留最大倍数，不保留多段独立事件；若需事件序列需另定义输出口径，当前不扩展。
- 后续用户要求多次记录，已改为逐次触发输出，新增 cooldown_days=5，threshold 默认2可调；实现边界为触发后跳过5个日历交易日、第6日起可再次触发，允许持续达标重复记录，每次调用冷却独立。同步原说明及真实DB调试入口，AST通过，未执行运行验证。保留短表既往查询有数据与用户当前描述的差异，不自行认定已清空。
- 用户要求将后续讨论直接维护在所属模块 Markdown。已在 scoring/scoring.md 保留原始提问并补入等权概念指数、窗口中位数、参与度、覆盖率、非重叠窗口比较、确认日期及历史成员偏差等建议，列出待确认事项；未修改业务代码或执行实验。长期约定已同步。
- 已在 scoring/scoring.md 第7点原位回复“等权是什么意思”，解释每日等权、日涨跌幅平均与累计复利、与市值加权及初始等额持有的区别，并保留用户问题和未确认状态。
- 根据用户新增回复，在原讨论文档补充三种聚合方法评价、停牌剔除与复牌边界、历史成员回溯解释、可配置40日窗口、环比候选及覆盖率说明。强调同窗收益决定同窗权重是描述性评分，不是可实现收益；层内等权加层间市值权重不等同整体市值加权。保留原话并更新确认表，未执行实验或修改代码。
- 用户在 scoring/scoring.md 确认只剔除停牌当天、市值加权采用流通市值，本版不考虑历史成员回溯、缺失专项治理、最小成员数和覆盖率筛选。已原位回复并同步确认表，保留基本不可计算边界；不重复追问已排除事项，未实现或运行。
- 按用户要求将 scoring/scoring.md 原位整理为功能设计、数据边界、公式与方法详细设计、环比与阶段研究、参数输出草案、验证约束及待定清单。删除文档内问答记录，保留需求结论且明确建议与确认状态；未删除聊天会话，未新增副本或修改代码。
