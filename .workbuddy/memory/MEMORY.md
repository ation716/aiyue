# 项目长期约定

- 2026-09-22 用户明确长期授权：涉及数据库读取的代码每次修改后，AI须主动真实只读运行验证，不只做AST检查；此授权不含数据库写入或任意实验。用户“不计算ST和*ST”是排除对象，不是统一按普通比例计算；旧实现/旧日志相反解释有误，尚需完成排除及同范围收盘明细校验。

- 2026-09-22 用户要求每日双时点统计的涨跌停数量不再查询异动明细，而是开盘、收盘都依据当日价格与前一交易日收盘价及限制比例自行计算；比例、价格最小单位、特殊状态处理待用户确认。

- 2026-09-22 用户要求调试入口简短：配置和导入之外约三行调用，用断点查看变量，不要入口打印、复杂对照、主动raise或断言流程；注释须说明配置及各参数选项含义。函数内部必要输入保护不因此移除。概念涨幅第二方法确认使用当日流通市值（current），用于当日描述统计，不再默认要求前日权重。

- 2026-09-22 用户补充文档问答格式：在文末以 `ask：` 加问题，AI 在原问题后就近回复；需要用户确认时，在回复末尾写 `reply：` 及待确认项。保留原问，建议不视为已确认。

- 2026-09-21 用户约定：后续在模块 Markdown 中以 `demand:` 或 `demand：` 标记新需求；就近完善需求、区分已确认与建议，保留用户原文，未明确口径先在原文档提问，不另建重复需求文档。

- 2026-09-21 用户要求后续在 Markdown 中讨论：问题、完整回复、口径确认与修订就近维护在所属模块已有 Markdown 中，聊天仅简短确认更新与必要阻塞；保留原始提问，区分建议与已确认决定，不为交付新增同主题副本。

- 2026-09-21 用户明确要求：测试与调试必须使用真实数据库或用户提供的数据；未经明确允许不得创建人工、随机、模拟数据。数据不可用时停止并说明，不用合成样本兜底。

- 用户于 2026-09-20 明确要求：后续回答与协作必须遵循项目 `ai_rules/` 下的全部规则。每次接手先读取该目录索引与对话规则，涉及实验时再读取追溯规则，以文件最新版本为准。
- 实际规则目录为 `ai_rules/`，不是旧入口说明中的 `rules/`。
- 2026-09-21 用户明确修订：仅与用户对话的文字按 R1 使用 token、代号和抽象描述；项目内代码、配置、Markdown 及其他文档不脱敏，使用真实命名和正常专业术语，保证可读性；引用到对话时再脱敏。普通文档仍不存储密码、密钥。
- 按 R2 执行 G1/G2/G3：先记完整配置及差异，再提供命令；脚本由用户执行；登记实际产出后由用户选择分析方式；结论、证据、后续动作写回才闭环。历史产物不删除，不引用来源不明结果作为证据。
- 用户于 2026-09-21 要求保持项目简洁清晰：模块相关 Markdown 就近放入所属模块目录；优先更新已有文档，每个主题只维护一份，不为交付展示另建重复副本。根目录只保留必要总览和入口，专用规则及追溯目录沿用原约定；避免无必要的辅助脚本、映射文件和总结文件。此约定不等于授权批量删除已有文件。
