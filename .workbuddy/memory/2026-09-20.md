# 2026-09-20

- 按需求稿完成工程骨架：覆盖 61 个必需文件与 8 个数据/输出子目录；保留原需求稿和规则文件。
- 配置与日志采用标准库实现，凭据从环境变量读取且不进入默认对象表示。业务接口显式未实现，四组验证用例显式跳过。
- 本次仅做文件结构、AST、笔记本 JSON 与说明文字检查，未导入业务模块、未连接外源、未运行实验；不可视为业务验证通过。
- 需求稿输出目录只保留占位，新实验继续遵循 R2；正式结果路径属性已预留，自动门禁与档案尚待业务实现阶段补齐。
- 按 scoring/scoring.md 新增 scoring/concept_gain.py，提供等权、显式市值时点加权、可选四层独立研究和相邻窗口比较；未知状态保守传播，不擅自认定停牌或复牌衔接。真实数据库参考测试集中在 __main__；真实概念名称须由用户填写，未生成模拟数据。同步更新原设计文档的实现说明。仅完成 AST 语法检查，未连接数据库、未执行业务测试；实际运行仍需先登记配置。
