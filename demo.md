现在我要建立一个可运行的demo，
选股策略（写在 scoring 下），价格破60日线，60日价格中位数小于60日平均价格的90%，近5 日累计跌幅小于5%，筛选结果按换手率降序，市值升序排列（市值必须大于20亿）
选股和持仓采用 portfolio/pfo_design.md 设计的方法，方法写在 portfolio 下
在backtest 下写好回测，根据 backtest/backtest_design.md 里的交易规则，条件买单为上一日收盘价的 99.5%，条件卖单为收益 >=10%，或者持股日期大于等于 10 日，止损买单为亏损 -10%，回测调用 portfolio 的方法，
最终测试 2024-01-01 到 2026-09-17 的数据，为进行数据准备交易时间在 2024-01-01之后的60个交易日，本金20万最后的数据画好收益曲线图和交易记录


ask:
你是一个有 10 年交易经验的A股老股民，一些问题你需要根据经验自己判断
对于1，2，3，4 问题
1. 向上穿越
2. 只要大于 -5%
3. 已有 slot 的 gain <= -10% 时触发 sl sell
4. 接受
现在需要你用代码实现demo的所有要求，中间遇到问题以股民经验解决，开始干吧

## 2026-09-23 实现补充（保留以上原始要求）

### 已落实口径
- `scoring/demo.py::demo_candidates`：t日close向上穿越60有效日均线，t-1日close不高于其旧均线；60有效日中位数严格小于均值×0.9；t/t-5变化严格大于-5%。t-1、t-5按统一日期轴，不跨缺日取旧值。按turn降序、规模升序稳定排序。
- 日线只有`float_mv`（元），本版明确用**流通市值估算值>20亿元**；未提供完整总市值，不能冒充总市值过滤。`turnover_rate`按小数比例原样排序。
- `portfolio/demo.py`保留`stock_keep`、`position_managent`、`ultimate_distribute`原拼写；8减去已有对象数，候选去重且过滤已有对象，选完再等分剩余现金。执行层按100整数单位取整并预留配置费用。
- `backtest/demo.py`：open→intraday→close，各阶段先卖后买；t日收盘信号仅t+1使用，未成交信号次日失效。盘中卖出所得不用于同一盘中阶段新买，保留至尾端；卖出当日不再买同对象。开盘/尾端先卖后买为明确的阶段内模型假设，不证明现实竞价可如此再提交订单。
- 条件买价=前日close×0.995，tp=买入价×1.1，sl=买入价×0.9；T+1。持有日数按统一交易日含买入日，满10日在尾端尝试退出，受限顺延。开盘跳空sl按开盘价，未知盘中路径sl优先。
- 价格用Decimal ROUND_HALF_UP取分。竞价条件包含相等；买不能在上限及以上，卖不能在下限及以下。盘中价格严格位于low/high之间。按原稿high==close、low==close作封死代理并执行例外（low<open、high>open）；这些等式本身不等于确认真实封板。
- 2024-01-01起的前60个统一交易日仅预热，统一日历为过滤对象前日线日期并集。数据须覆盖2026-09-17；初始200000。缺日禁止成交；期末不强制清仓，缺日持仓按最后已知close估值并记录陈旧估值。

### 明确假设和局限
- 费用、最低费用、滑点均设0，是**零摩擦演示，不是净真实可实现回报**，没有捏造官方收费。
- 仅主板代码前缀；当前名称含ST/*ST排除，无名称对象排除。数据库缺可靠历史状态，不能声称全部排除历史ST；当前名称映射带来时点/幸存者偏差。
- 数据价格为前复权；固定10%及分位限制价不能还原所有真实限制状态。没有分钟路径、队列和容量数据，成交仅是用户指定日K规则的演示。日终vol/amount仅事后有效性校验，非当时可见信号。
- 不构造人工/随机/模拟数据，不写DB，不执行旧脚手架，不合并目录，不改其他算法。读DB超时无自动重试。

### 运行方法
- 配置全快照及本次授权例外见`traces/demo.md`，不修改R2全局。
- 运行：`C:/Users/28414/.workbuddy/binaries/python/envs/default/Scripts/python.exe scripts/run_demo.py --experiment E02`
- 独立结果复核：同解释器运行 `scripts/run_demo.py --validate-only results/demo/E02/<run_id>`；入口默认实验编号为E02，也可显式指定其他已登记实验节。
- 本轮结果保存至`results/demo/E02/<run_id>/`（实际目录严格绑定所选实验），静态自含SVG HTML副本位于`outputs/<run_id>/demo.html`。包含完整JSON交易记录与HTML交易表，不生成CSV。
- 脚本执行余额、逐笔现金、总值、100整数单位、数量上限、T+1、信号紧邻前日、阶段顺序断言；真实数据前半区间截断重算检查未来数据不改变过去候选；AST静态检查与业务验证分开记录。
- 实际E02运行于2026-09-23成功：真实日期覆盖2024-01-02至2026-09-17，共658个统一日期；33个月读取，原始2,071,375行，过滤后1,529,854行、2,358个代码；13条信号、11条记录，期末总值184,938.00，区间结果-7.531%。独立复核返回passed；产出位于`results/demo/E02/20260923_222307_092589/`，交付副本为`outputs/20260923_222307_092589/demo.html`。
